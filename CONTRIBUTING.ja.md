# コントリビューション

ご関心をお寄せいただきありがとうございます。[English version](./CONTRIBUTING.md)

## 環境構築

```bash
pnpm install
pnpm test        # Vitest（property-based test を含む）
pnpm typecheck
pnpm lint        # Biome。適用は pnpm lint:fix
pnpm build
pnpm size        # サイズ目標に対する gzip レポート

pnpm dev:vanilla # フレームワークなしの例
pnpm dev:react   # React の例
pnpm docs:dev    # ドキュメントサイト（パッケージをソースから直接実行）
```

Node 20 以降と pnpm が必要です。テストはワークスペースのパッケージをソースに解決するため、事前ビルドは不要です。

## 4つの不変条件

`core` または `ui` に手を入れる前に、以下を把握してください。**これらはスタイルの好みではありません。** ライブラリの性能とアクセシビリティの主張がこの上に成り立っており、いずれかを壊す変更は「ライブラリが何であるか」を変える変更です。

1. **Canvas には UI を描かない。** 選択枠・ハンドル・ガイドは DOM 要素です。Canvas に描けば CSS で変更できず、支援技術からも見えなくなります。
2. **非選択の図形には操作 UI の DOM を作らない。** 図形が1万個あっても操作 UI は数個です。DOM ノード数が文書サイズに比例する実装はこれを壊します。
3. **ビューポート変換は1フレームに1回、1要素にだけ書く。** オーバーレイのコンテナが担い、子はワールド座標で配置します。
4. **ハンドルのサイズ補正は CSS 変数 `--hc-zoom` で行う。** JavaScript から各要素にサイズを書くと、不変条件3の意味がなくなります。

4つとも `packages/ui/test/invariants.test.ts` にテストがあります。**変更によってこれらが落ちる場合、間違っているのは変更のほうであってテストではありません。**

## 意図的に対象外としているもの

以下を実装する前に、まず Issue で議論してください。

- Fabric.js との機能パリティ
- リッチテキスト編集エンジン
- 画像フィルタ・エフェクト
- ツールバー・カラーピッカー・レイヤーパネルなどのアプリケーション UI

HeadlessCanvas はエンジンであってアプリではありません。線引きの基準は「その上に作られるすべてのエディタに必要か？」です。

## 図形の追加

図形は登録制で、**組み込み図形もまったく同じ仕組みを使っています**（特別扱いの経路はありません）。`ShapeUtil` を実装して登録してください。

```ts
const wallUtil: ShapeUtil<WallShape> = {
  type: 'wall',
  propsVersion: 1,
  getDefaultProps: () => ({ thickness: 8 }),
  render(shape, ctx, info) { /* ... */ },
  hitTest(shape, point, tolerance) { /* ... */ },
}
```

**`hitTest` で 2D コンテキストを使わないでください。** ヒットテスタはコンテキストを持ちません。`packages/core/src/shape/shapes/path.ts` のように幾何で直接判定します。

**`propsVersion` は最初から付けてください。** 公開後に props は必ず変化し、後から追加した移行機構では既に書き出された文書を救えません。

## ツールの追加

ツールは操作モードを担います。`Tool` を実装して登録してください。

```ts
editor.tools.register('place', (editor) => new PlaceTool(editor))
```

**ジェスチャ中の変更は `editor.setEphemeral()` を通し、`editor.commitEphemeral()` で確定させてください。** ポインタ移動のたびに文書へ書き込むと、毎フレーム状態木を再構築することになり、1つのジェスチャが数百の Undo ステップになります。

## スタイル

- 整形と Lint は Biome。push 前に `pnpm lint:fix` を実行してください
- **`core` は実行時依存ゼロ**です。依存を追加するには強い理由が必要です
- コメントは *why* を書き、*what* は書かないでください。奇妙に見えるコードには「これがないと何が壊れるか」を書きます
- **このリポジトリの記述はすべて英語**です（識別子・コメント・コミットメッセージ・ドキュメント）。日本語訳は `.ja` サフィックスで英語版と併置し、**同一コミットで両方を更新**します

## テスト

- 数学と順序付けは property-based test（`fast-check`）で検証します。性質を書けるところでは例の列挙より性質を優先してください
- 操作は**実際のポインタ・キーボードイベントを流して**検証します（内部関数の直接呼び出しではなく）。`packages/ui/test/tools.test.ts` を参照
- 性能ガードは**処理の形**を検証します（画面外の図形がカリングされること、ドラッグが確定状態に触れないこと）。CI で再現しない実時間は検証しません

## リリース

Changesets を使います。`pnpm changeset` で、**利用者から見た変更**を記述してください。

**公開 API には CSS 契約も含まれます** — クラス名、`data-hc-*` 属性、文書化されたカスタムプロパティ。いずれかの改名は破壊的変更です。

## 行動規範

参加により [Code of Conduct](./CODE_OF_CONDUCT.md) に同意したものとみなされます。
